import React from 'react';

const Display = ({strike, ball}) => {

    return (
        <div>
            <h4>Strike: {strike}</h4>
            <h4>Ball: {ball}</h4>
        </div>
    )
}

export default Display;
import React from 'react';
import Button from './Button';

function Dashboard() {
    return (
        <div className='buttons'>
            <ChangeButton
            name='Strike'
            value={props.strikes}
            strike={propls.strikes}
            />
        </div>
    )
}